{
  "name": "Electron messenger",
  "version": "0.1.1a",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . electron-messenger --all --version=0.33.3 --app-version=0.1.0 --out=dist --ignore=dist --prune"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-cli": ">= 0.1.6",
    "grunt-contrib-concat": "^0.5.1",
    "grunt-contrib-cssmin": ">=0.5.0",
    "grunt-contrib-uglify": "^0.9.2",
    "grunt-contrib-watch": "~0.3.1"
  }
}
